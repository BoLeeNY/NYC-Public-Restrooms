<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css">
    <link rel="stylesheet" href="./styles.css">
    <title>Document</title>
</head>
<body>
    <section class="hero is-danger is-medium is-bold">
        <div class="hero-body">
          <div class="container">
            <h1 class="title">
                <a href="/parks">
                    NYC Public Toilets
                </a>
            </h1>
            <h2 class="subtitle">
              Edit A Toilet
            </h2>
          </div>
        </div>
        <div class="hero-foot">
            <nav class="tabs">
              <div class="container">
                <ul>
                  <li class="is-active">
                      <a href="/parks/new">Post a New Listing</a></li>
                  <li><a>Search</a></li>
                </ul>
              </div>
            </nav>
        </div>
      </section>

    <div class="column is-large">
        <form method="POST" action="/parks/<%=parks.id %>?_method=PUT">
            <label for="name">
              Name:
              <input value="<%=parks.name %>" type="text" name="name" id="name">
            </label>
            <br>
            <label for="location">
                Location:
              <input value="<%=parks.location %>" type="text" name="location">
            </label>
            <br>
            <label for="borough">
                Borough:
                <input value="<%=parks.borough %>" type="text" name="borough">
            </label>
            <br>
            <label for="open">
                Is It Open?
                <input type="checkbox" name="open" value="<%=parks.open %>"
                "<% if (parks.open === 'Yes') {%> checked <%}%>" >
            </label>
            <label for="handicap">
                Handicap Accessible?
                <input type="checkbox" name="handicap" value="<%=parks.handicap %>"
                "<% if (parks.handicap === 'Yes') {%> checked <%}%>">
            </label>
            <input class="button is-info is-medium" type="submit" value="Update">
          </form>
    </div>
    </form>
</body>
</html>